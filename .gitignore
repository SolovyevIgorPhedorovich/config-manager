# ── Agent / IDE ────────────────────────────────────────────
.claude/
.idea/
*.iml
.vscode/

# ── Backend: Gradle / сборка ───────────────────────────────
backend/build/
backend/bin/
backend/.gradle/
.gradle/
!gradle/wrapper/gradle-wrapper.jar

# ── Frontend: Node / Vite ──────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.vite/
**/node_modules/

# ── Логи / временное ───────────────────────────────────────
*.log
*.tmp

# ── OS ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Containerlab: сгенерированный вывод и результаты тестов ──
# Создаётся при `clab deploy` (инвентари, TLS-ключи, topology-data) и прогонах
# нагрузочных тестов — не исходники, содержит секреты (.tls/ca/ca.key).
clab/topologies/clab-*/
clab/tests/load/results/
